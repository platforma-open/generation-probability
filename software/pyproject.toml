[project]
name = "software"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "olga>=1.3.0",
  "polars[rtcompat]>=1.43.0",
]

[dependency-groups]
dev = [
  "ruff>=0.15.22",
  "ty>=0.0.62",
]

[tool.ruff.lint]
select = ["ALL"]
ignore = ["D", "T20"]
