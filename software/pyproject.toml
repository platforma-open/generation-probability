[project]
name = "software"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "olga>=1.3.0",
  "polars[rtcompat]>=1.43.0",
]

[dependency-groups]
dev = [
  "ruff>=0.16.0",
  "ty>=0.0.63",
]

[tool.ruff.lint]
select = ["ALL"]
ignore = ["CPY", "D", "T20"]
